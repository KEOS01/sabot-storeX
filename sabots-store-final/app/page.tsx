'use client';
import React from 'react';
// ⚠️ Ici tu colles tout ton code "BoutiqueSabots" (celui qu'on avait dans la canvas)
// Pour gagner du temps, tu peux remplacer ce fichier par la version complète déjà générée.

import BoutiqueSabots from './BoutiqueSabots';

export default function Page() {
  return <BoutiqueSabots />;
}
